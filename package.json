{
  "name": "gqelm",
  "version": "1.0.0",
  "description": "GraphQL playground implementation",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -r dist && parcel build src/index.html --public-url ./",
    "test": "elm-test",
    "autotest": "elm-test --watch",
    "api": "elm-graphql https://elm-graphql.herokuapp.com --base StarWars --output src"
  },
  "author": "Robinson A Ramirez",
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-hot": "^1.0.1",
    "elm-test": "^0.19.0-rev6",
    "node-elm-compiler": "^5.0.4",
    "parcel-bundler": "^1.12.0"
  }
}
